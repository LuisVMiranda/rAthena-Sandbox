diff --git a/Codex diffs/Custom drops mapflag [OK]/README.md b/Codex diffs/Custom drops mapflag [OK]/README.md
index 6028cdf..242dd44 100644
--- a/Codex diffs/Custom drops mapflag [OK]/README.md	
+++ b/Codex diffs/Custom drops mapflag [OK]/README.md	
@@ -23,7 +23,7 @@ After applying the diff:
 - Two configuration modes:
   1. Legacy script mapflag line: `<mapname> mapflag mobdrop <item_id>,<rate>{,<mob_id>}`
   2. YAML database (`db/mapflag_mobdrop.yml`) with:
-     - map
+     - map (specific map name or global `all`/`*`)
      - single monster OR monster list
      - dynamic rate (`Min`/`Max`)
      - bind mode (free/account/char)
diff --git a/Codex diffs/Custom drops mapflag [OK]/assets/IMPLEMENTATION_NOTES.md b/Codex diffs/Custom drops mapflag [OK]/assets/IMPLEMENTATION_NOTES.md
index 976285a..7e01811 100644
--- a/Codex diffs/Custom drops mapflag [OK]/assets/IMPLEMENTATION_NOTES.md	
+++ b/Codex diffs/Custom drops mapflag [OK]/assets/IMPLEMENTATION_NOTES.md	
@@ -32,3 +32,5 @@ Create a mapflag system (`mobdrop`) that supports both legacy script mapflag lin
 - Keeps legacy `mapflag mobdrop` parser behavior.
 - YAML and legacy rules are additive.
 - No SQL migration required.
+
+- Map supports `all` or `*` to apply a rule globally on all maps.
diff --git a/Codex diffs/Custom drops mapflag [OK]/required files/db/mapflag_mobdrop.yml b/Codex diffs/Custom drops mapflag [OK]/required files/db/mapflag_mobdrop.yml
index 54b12ee..8031f2f 100644
--- a/Codex diffs/Custom drops mapflag [OK]/required files/db/mapflag_mobdrop.yml	
+++ b/Codex diffs/Custom drops mapflag [OK]/required files/db/mapflag_mobdrop.yml	
@@ -22,6 +22,15 @@
 #   - Character, character, Char, char, char_bound, BOUND_CHAR
 #
 # ---------------------------------------------------------------------------
+# MAP TARGET BEHAVIOR
+# ---------------------------------------------------------------------------
+# Map accepts either a specific map name or global keywords:
+#   - all
+#   - *
+#
+# Using all/* applies the rule globally across every map.
+#
+# ---------------------------------------------------------------------------
 # MONSTER FILTER BEHAVIOR
 # ---------------------------------------------------------------------------
 # You can choose EITHER:
@@ -51,7 +60,7 @@ Body:
   # ============================================================
   # COMPLETE BOILERPLATE (PRODUCTION TEMPLATE)
   # ============================================================
-  # - Map: <map_name>
+  # - Map: <map_name | all | *>
   #   Monster: <mob_aegis_name | All | *>           # optional
   #   Monsters:                                     # optional (use instead of Monster)
   #     - <mob_aegis_name>
@@ -64,8 +73,8 @@ Body:
   #   Bind: <see accepted values above>             # optional (defaults to Free)
   #   RandomOptionGroup: <group_name>               # optional
 
-  # Example A: any monster in prontera, fixed 2.50%, free-trade item drop
-  # - Map: prontera
+  # Example A: any monster on all maps, fixed 2.50%, free-trade item drop
+  # - Map: all
   #   Monster: All
   #   Item: Apple
   #   Rate:
diff --git a/db/mapflag_mobdrop.yml b/db/mapflag_mobdrop.yml
index 54b12ee..8031f2f 100644
--- a/db/mapflag_mobdrop.yml
+++ b/db/mapflag_mobdrop.yml
@@ -22,6 +22,15 @@
 #   - Character, character, Char, char, char_bound, BOUND_CHAR
 #
 # ---------------------------------------------------------------------------
+# MAP TARGET BEHAVIOR
+# ---------------------------------------------------------------------------
+# Map accepts either a specific map name or global keywords:
+#   - all
+#   - *
+#
+# Using all/* applies the rule globally across every map.
+#
+# ---------------------------------------------------------------------------
 # MONSTER FILTER BEHAVIOR
 # ---------------------------------------------------------------------------
 # You can choose EITHER:
@@ -51,7 +60,7 @@ Body:
   # ============================================================
   # COMPLETE BOILERPLATE (PRODUCTION TEMPLATE)
   # ============================================================
-  # - Map: <map_name>
+  # - Map: <map_name | all | *>
   #   Monster: <mob_aegis_name | All | *>           # optional
   #   Monsters:                                     # optional (use instead of Monster)
   #     - <mob_aegis_name>
@@ -64,8 +73,8 @@ Body:
   #   Bind: <see accepted values above>             # optional (defaults to Free)
   #   RandomOptionGroup: <group_name>               # optional
 
-  # Example A: any monster in prontera, fixed 2.50%, free-trade item drop
-  # - Map: prontera
+  # Example A: any monster on all maps, fixed 2.50%, free-trade item drop
+  # - Map: all
   #   Monster: All
   #   Item: Apple
   #   Rate:
diff --git a/src/map/mob.cpp b/src/map/mob.cpp
index 9ac4fd5..3b16830 100644
--- a/src/map/mob.cpp
+++ b/src/map/mob.cpp
@@ -3465,6 +3465,8 @@ int32 mob_dead(mob_data *md, block_list *src, int32 type)
 		}
 
 		// Process YAML-driven MF_MOBDROP rules
+		std::vector<std::shared_ptr<s_mapflag_mobdrop_db>> yaml_rule_sets;
+		std::shared_ptr<s_mapflag_mobdrop_db> yaml_global = mapflag_mobdrop_db.find(UINT16_MAX);
 		std::shared_ptr<s_mapflag_mobdrop_db> yaml_mobdrop;
 
 		if (map[md->m].instance_id > 0)
@@ -3472,8 +3474,13 @@ int32 mob_dead(mob_data *md, block_list *src, int32 type)
 		else
 			yaml_mobdrop = mapflag_mobdrop_db.find(md->m);
 
-		if (yaml_mobdrop != nullptr) {
-			for (const s_mapflag_mobdrop_rule& rule : yaml_mobdrop->rules) {
+		if (yaml_mobdrop != nullptr)
+			yaml_rule_sets.push_back(yaml_mobdrop);
+		if (yaml_global != nullptr)
+			yaml_rule_sets.push_back(yaml_global);
+
+		for (const std::shared_ptr<s_mapflag_mobdrop_db>& rule_set : yaml_rule_sets) {
+			for (const s_mapflag_mobdrop_rule& rule : rule_set->rules) {
 				if (!rule.mob_ids.empty() && std::find(rule.mob_ids.begin(), rule.mob_ids.end(), md->mob_id) == rule.mob_ids.end())
 					continue;
 
@@ -7095,7 +7102,7 @@ uint64 MapDropDatabase::parseBodyNode( const ryml::NodeRef& node ){
 	}
 
 	if( !exists ){
-		this->put( mapid, mapdrops );
+		this->put( map_key, mapdrops );
 	}
 
 	return 1;
@@ -7199,26 +7206,37 @@ uint64 MapFlagMobDropDatabase::parseBodyNode(const ryml::NodeRef& node){
 		return 0;
 	}
 
-	uint16 mapindex = mapindex_name2idx( mapname.c_str(), nullptr );
+	uint16 map_key = 0;
+	bool is_global = false;
 
-	if( mapindex == 0 ){
-		this->invalidWarning( node["Map"], "Unknown map \"%s\".\n", mapname.c_str() );
-		return 0;
-	}
+	if( strcmpi( mapname.c_str(), "all" ) == 0 || mapname == "*" ){
+		map_key = UINT16_MAX;
+		is_global = true;
+	}else{
+		uint16 mapindex = mapindex_name2idx( mapname.c_str(), nullptr );
 
-	int16 mapid = map_mapindex2mapid( mapindex );
+		if( mapindex == 0 ){
+			this->invalidWarning( node["Map"], "Unknown map "%s".\n", mapname.c_str() );
+			return 0;
+		}
 
-	if( mapid < 0 ){
-		// Silently ignore. Map might be on a different map-server.
-		return 0;
+		int16 mapid = map_mapindex2mapid( mapindex );
+
+		if( mapid < 0 ){
+			// Silently ignore. Map might be on a different map-server.
+			return 0;
+		}
+
+		map_key = static_cast<uint16>( mapid );
 	}
 
-	std::shared_ptr<s_mapflag_mobdrop_db> mapdrops = this->find( mapid );
+	std::shared_ptr<s_mapflag_mobdrop_db> mapdrops = this->find( map_key );
 	bool exists = mapdrops != nullptr;
 
 	if( !exists ){
 		mapdrops = std::make_shared<s_mapflag_mobdrop_db>();
-		mapdrops->mapid = mapid;
+		mapdrops->mapid = map_key;
+		mapdrops->is_global = is_global;
 	}
 
 	s_mapflag_mobdrop_rule rule = {};
@@ -7238,7 +7256,7 @@ uint64 MapFlagMobDropDatabase::parseBodyNode(const ryml::NodeRef& node){
 			std::shared_ptr<s_mob_db> mob = mobdb_search_aegisname( mobname.c_str() );
 
 			if( mob == nullptr ){
-				this->invalidWarning( node["Monster"], "Unknown monster \"%s\".\n", mobname.c_str() );
+				this->invalidWarning( node["Monster"], "Unknown monster "%s".\n", mobname.c_str() );
 				return 0;
 			}
 
@@ -7263,7 +7281,7 @@ uint64 MapFlagMobDropDatabase::parseBodyNode(const ryml::NodeRef& node){
 			std::shared_ptr<s_mob_db> mob = mobdb_search_aegisname( mobname.c_str() );
 
 			if( mob == nullptr ){
-				this->invalidWarning( mobNode, "Unknown monster \"%s\".\n", mobname.c_str() );
+				this->invalidWarning( mobNode, "Unknown monster "%s".\n", mobname.c_str() );
 				return 0;
 			}
 
@@ -7283,7 +7301,7 @@ uint64 MapFlagMobDropDatabase::parseBodyNode(const ryml::NodeRef& node){
 		std::shared_ptr<item_data> item = item_db.search_aegisname( itemname.c_str() );
 
 		if( item == nullptr ){
-			this->invalidWarning( node["Item"], "Unknown item \"%s\".\n", itemname.c_str() );
+			this->invalidWarning( node["Item"], "Unknown item "%s".\n", itemname.c_str() );
 			return 0;
 		}
 
@@ -7308,7 +7326,7 @@ uint64 MapFlagMobDropDatabase::parseBodyNode(const ryml::NodeRef& node){
 			int64 group_constant;
 
 			if( !script_get_constant( group_name.c_str(), &group_constant ) ){
-				this->invalidWarning( node["ItemGroup"], "Unknown item group constant \"%s\".\n", group_name.c_str() );
+				this->invalidWarning( node["ItemGroup"], "Unknown item group constant "%s".\n", group_name.c_str() );
 				return 0;
 			}
 
@@ -7359,7 +7377,7 @@ uint64 MapFlagMobDropDatabase::parseBodyNode(const ryml::NodeRef& node){
 		}else if( bind == "Free" || bind == "free" || bind == "Trade" || bind == "trade" || bind == "none" || bind == "BOUND_NONE" ){
 			rule.bound = BOUND_NONE;
 		}else{
-			this->invalidWarning( node["Bind"], "Invalid Bind value \"%s\". Use Free/Trade/None, Account or Character.\n", bind.c_str() );
+			this->invalidWarning( node["Bind"], "Invalid Bind value "%s". Use Free/Trade/None, Account or Character.\n", bind.c_str() );
 			return 0;
 		}
 	}
@@ -7372,7 +7390,7 @@ uint64 MapFlagMobDropDatabase::parseBodyNode(const ryml::NodeRef& node){
 		}
 
 		if( !random_option_group.option_get_id( group_name, rule.randomopt_group ) ){
-			this->invalidWarning( node["RandomOptionGroup"], "Unknown random option group \"%s\".\n", group_name.c_str() );
+			this->invalidWarning( node["RandomOptionGroup"], "Unknown random option group "%s".\n", group_name.c_str() );
 			return 0;
 		}
 	}
@@ -7380,7 +7398,7 @@ uint64 MapFlagMobDropDatabase::parseBodyNode(const ryml::NodeRef& node){
 	mapdrops->rules.push_back( rule );
 
 	if( !exists ){
-		this->put( mapid, mapdrops );
+		this->put( map_key, mapdrops );
 	}
 
 	return 1;
diff --git a/src/map/mob.hpp b/src/map/mob.hpp
index 05a9c8f..2be1fac 100644
--- a/src/map/mob.hpp
+++ b/src/map/mob.hpp
@@ -324,6 +324,7 @@ struct s_mapflag_mobdrop_rule {
 
 struct s_mapflag_mobdrop_db {
 	uint16 mapid;
+	bool is_global;
 	std::vector<s_mapflag_mobdrop_rule> rules;
 };
 
